# Archived pipeline versions

Superseded, kept for history only — nothing in the project imports these.

- `cpu_pipeline_v1.py` / `cpu_pipeline_v2.py` — earlier iterations of the CPU
  anomaly pipeline. **`eda/cpu_pipeline_v3.py` is canonical** — it's the only
  version anything (`eda/cpu_evaluate.py`, `eda/disk_pipeline.py`) imports
  from.
